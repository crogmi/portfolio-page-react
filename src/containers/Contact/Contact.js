import React from "react";

const Contact = () => {
    return (
        <section className="main-info contact">
            <h1 className="section-title">Contact</h1>
        </section>
    );
};

export default Contact;